import { View, Text, StyleSheet, Switch, ImageBackground, useColorScheme, useWindowDimensions } from 'react-native';
import react, {useState} from 'react'

import {styles} from './styles';

export default function Footer() {

  return (
    <View style={{flex: 0.1, backgroundColor: '#E97132'}}>
    </View>
  );
}
